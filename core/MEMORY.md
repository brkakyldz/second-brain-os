<!--
  Index of durable facts — pointers, not detail. One fact per line; when detail
  exists, end the line with a pointer into notes/.
  Budget: 4000 characters, counted the way the hook counts them
  (`countBudgetChars` in `.claude/hooks/lib.mjs`): HTML comments stripped, so
  this block does not count. Never silently truncate — consolidate before
  adding. Every fact here must be timeless, dated, or point to a live source.
  Two things are never entries here: the vault's own construction history (give
  it its own note) and open work items (`PROPOSALS.md`).
-->

# Memory

<!-- Example of the shape, delete once you have real ones:
- Project X moved to trunk-based development on 2026-01-14 → [[project-x]]
-->
