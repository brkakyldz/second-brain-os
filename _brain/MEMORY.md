<!--
  Format: one fact per line. When detail exists, end the line with a pointer,
  e.g. "- Project X uses trunk-based dev → [[_brain/memory/project-x]]".
  Budget: 100 lines max. Never silently truncate — when over budget,
  consolidate (merge/archive) before adding anything new. Every fact here
  must be timeless, dated, or a pointer to a live source.
-->

# Memory
